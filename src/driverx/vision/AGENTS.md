# Vision AGENTS.md

- This module creates evidence artifacts, not model features.
- Keep rendering deterministic for QA.
- Do not require browser, OpenCV, matplotlib, or Waymo dependencies for fixture runs.
