# Data

Local Waymo E2E data mounts belong here or at a path configured outside the
repo. Do not commit TFRecords, downloaded archives, or extracted dataset shards.

Future code should read data paths from config or environment variables.
