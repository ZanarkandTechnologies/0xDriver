dataset:
  kind: fixture
  name: construction_merge
  # For future real Waymo runs:
  # kind: waymo
  # path: ${WAYMO_E2E_DATASET}

reasoner:
  backend: mock
  uncertainty: 0.34

output:
  root: artifacts/runs
  run_id:

author: 0xDriver
method_name: fixture_vla_intent_planner
